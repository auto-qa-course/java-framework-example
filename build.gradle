apply plugin: 'java'


dependencies {
    testImplementation 'org.testng:testng:6.8.8'
}

repositories {
    mavenCentral()
}

test {
    useTestNG() {
        setParallel "methods"
        setThreadCount 2
    }
    ext.workingDirectory = 'build/'
}